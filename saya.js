const profile = {
  nama: "Marselinus Jeffry Paramma",
  alamat: "Lr6A",
  instagram: "@mjep__",
};

console.log("=== Profil ===");
console.log(`Nama: ${profile.nama}`);
console.log(`Alamat: ${profile.alamat}`);
console.log(`Instagram: ${profile.instagram}`);

/*document.addEventListener('DOMContentLoaded', () => {
  const app = document.getElementById('app') || document.body;
  app.innerHTML = `
    <h1>Profil</h1>
    <p><strong>Nama:</strong> ${profile.nama}</p>
    <p><strong>Instagram:</strong> <a href="https://www.instagram.com/${profile.instagram.replace('@', '')}/" target="_blank">${profile.instagram}</a></p>
    <p><strong>Umur:</strong> ${profile.umur}</p>
  `;
});*/
